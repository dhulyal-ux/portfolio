# Supabase — copy to .env.local and fill in from your Supabase project settings.
# If these are left unset, the site automatically falls back to the built-in
# seed content in lib/content.ts, so it still renders locally without a database.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
